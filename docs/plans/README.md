# Plans

Index of design / implementation plans for `vista_eval_vlm`. Substance lives in each plan doc; this
table is the pointer. **Reviewed** = Phil has read the *current* content via `/read-plan` (or an
approved, SHA-in-sync `/explain-plan` HTML). `Stale` = was `Yes`, substantively edited since.

| Plan | Status | Reviewed | Description |
|---|---|---|---|
| [vlm-modular-preprocessing-and-context-viewer-roadmap.md](vlm-modular-preprocessing-and-context-viewer-roadmap.md) | Landed 2026-07-15 (Phase 2 landed 2026-07-16) | No | Dissolve the `experiment` god-enum into **ContextBlock modality-adapters** (leaf selectors: CT slice / pathology patch / EHR filter chain; `ordered` assembler + `supports_inline` seam, inline → Phase 1.5) + a weight-free **config-context viewer**. **EHR consumes full LUMIA timelines as input — filter + flat-render live at inference** (no offline prep, `meds_tools`/`meds_reader`/ontology dropped; serialization interrogation 2026-07-06). 6 review passes applied. Companion `.html` (in-sync `8567ac8decbc`) + review files under `reviews/`. **CT/substrate slice superseded 2026-07-07 →** `vlm-ct-feb26-v1_5-golden-rebaseline.md`. |
| [vlm-ct-feb26-v1_5-golden-rebaseline.md](vlm-ct-feb26-v1_5-golden-rebaseline.md) | Completed 2026-07-15 | Yes | **Supersedes the CT/substrate slice of the roadmap** (VM golden-smoke handback 2026-07-07). Now covers **rungs 1–2 only** (rung 0 split out 2026-07-08, LANDED GREEN 2026-07-13): `nov25` CT snapshot deleted → repoint CT resolution to the **v1_5/feb26 dataset link** (`image_study_uid`+`image_series_uid`; drop `DEFAULT_NIFTI_BUCKET_PREFIX`, add shared `resolve_ct_blob`), cut the eval substrate **v1_1→v1_5**, and **rebuild the Task-3b CT-dissolution byte-identity golden on feb26** (refactor axis byte-gated; substrate axis not). D3: `numeric_value` a declared delta. **Reviewed 2026-07-13** via `/review-plan` (Codex design + verification-design passes, both Revise → ~15 findings applied, 0 disagreements) → added `Out of scope` / `Landing & cleanup` / `Handoff phasing`; **OQ-Q1–Q5 resolved** (Q1 defer tooling, Q2 `run_bq`-only, Q3 executor-reports/Phil-decides-in-flight ~87% link-health prior, Q4 aggregate-only doc + detailed→GCP bucket, Q5 two vm-status docs). Companion `.html` **in-sync** `4a2d588ccea1`. |
| [vlm-rung0-reproduce-ryan-feb26.md](vlm-rung0-reproduce-ryan-feb26.md) | Completed 2026-07-15 | Yes | **Rung 0 — operability smoke that GATES the rebaseline above.** Reproduce Ryan's *weighted* pipeline (results CSV, not the weights-free golden harness) on a near-untouched **v1_1** substrate, changing only the CT storage prefix nov25→feb26 via a pulled-forward `ct_snapshot_prefix` config seam + leaving `ct_dir` unset (force-GCS, zero-code). Report milestone, not a byte/number gate. **All OQ-R1–R6 resolved:** mapping `axial_all_image↔image_and_timeline` / `no_image↔timeline_only` (`↔image_only` refuted); 0c report-only ~10% (soft); PFS-only first pass; **decoding = constrained** (`use_constrained_decoding_for_binary: true` to match Ryan's baseline — the sole committed `all_model_response.csv` is itself constrained). **Fresh-Claude `/review-plan` = Revise, all applied.** Approved via `/explain-plan` HTML, in-sync `5aea55ad1425` (2026-07-08). |
| [vlm-phase2-config-context-viewer.md](vlm-phase2-config-context-viewer.md) | Completed 2026-07-16 | Yes | **Phase 2 of the roadmap — the weight-free config-context viewer.** A CLI that renders exactly the `(config,task,experiment,model)` context each VLM receives (assembled prompt + post-window CT slices / pathology tiles as base64 thumbnails + token-budget bar) into one self-contained HTML, for **manual pre-run input-QA on a small subset** (inputs only, not scoring). Reuses golden's `PromptDataset` capture core (extract shared `context_capture.py`; golden byte-identity preserved) + two seams (`context_window` per adapter; standalone tokenizer-only counter). Codex `/review-plan` = Revise, core architecture CONFIRMED (renders off `PromptDataset`, not `Assembler`), ~10 findings applied. **3 residuals resolved 2026-07-15:** Q1 tokenizer cache-miss = hard STOP; Q2 CLI = `--type`/`--name` (Ryan's repo-wide convention); Q3 correctness coloring omitted. **Reviewed 2026-07-15** via `/explain-plan` (Phil approved the in-sync HTML; change-layer showed the 3 residual decisions). Companion `.html` in-sync at `9068e0a57dff` (review baseline). |
| [vlm-rung0-0b-decoding-fix.md](vlm-rung0-0b-decoding-fix.md) | Completed 2026-07-15 | Yes | **Fixes the class-3 deviation on the first 0b run** — constrained decoding never engaged: `run_bq.py:910` AND-gates the flag behind registry `is_binary`, and PFS is `is_binary:False` (3-class) → medgemma free-generated → ~54% `predicted_label==-1`. Amends **OQ-R6** of the rung-0 plan. **4 seams:** constrain off the Yes/No **mapping** (extract `is_binary_yes_no_task`→`task_mapping.py`) + per-task `[DECODE]` log + **fail-closed preflight** (RED before weights) + constrained reducer (registry-by-contract, rung0 `--output`, drop-count fix). Fresh 0b re-run required (not resumable). **Codex `/review-plan` = Revise, all applied; OQ-F1/F2/F3 resolved.** Approved via `/explain-plan` HTML, in-sync `58c1d5da` (2026-07-09). |
| [vlm-step5-lumia-live-ehr-adapter.md](vlm-step5-lumia-live-ehr-adapter.md) | Draft (2026-07-16) | Yes | **Step 5 of the roadmap — wires the already-built-but-unused `EHRAdapter`/filter chain into the real hot path**, replacing raw `patient_string` substitution with live LUMIA `.xml` parsing + filtering for 5 presets. **Fail-closed** on missing LUMIA files (Phil's call: exclude the row, don't fall back). Codex `/review-plan` ran two passes (main design + verification/handoff-design, both Revise, ~25 findings combined, 0 disagreements) — caught that the Phase 1 decision gate wasn't actually self-resolving (fail-closed filtering ran before the golden harness's sort+limit step, so before/after banks weren't guaranteed the same index set) and that an earlier truncation-idempotence claim was false. **Reviewed 2026-07-16** via `/explain-plan` — Phil's feedback consolidated `paths.lumia_corpus_dir` into the existing `retrieval.corpus_dir` key (one config path, not two) and resolved a coverage-scope question. Companion `.html` in-sync at `8f99261412a7`. **Implemented on `feat/lumia-live-ehr-adapter` (2026-07-17)**: `presets.py`/`run_bq.py`/`ehr.py`/2 docs done. **VM Phase 1 hit a class-3 deviation** (`docs/vm-status/2026-07-17-4989a20.md`): live LUMIA render vs. legacy `patient_string` near-disjoint (102/~24k shared lines) — byte-diff premise doesn't hold. **Verification & VM handoff for Phase 1 onward is now superseded by** `vlm-step5-lumia-demographics-flowsheet-replan.md` (see next row). |
| [vlm-step5-lumia-demographics-flowsheet-replan.md](vlm-step5-lumia-demographics-flowsheet-replan.md) | Draft (2026-07-17) | Yes | **Re-plan resolving the Step 5 deviation above.** Planner-side investigation (git blame + cross-repo reads of `meds2text`) split the gap into a **confirmed parser bug** (demographics — birthdate/ethnicity/gender live in a real, documented `<person>` XML element `parse_lumia` never reads; git blame + all plan/review docs show this was a plain oversight, not a design decision) and a **likely-intentional upstream exclusion** (STANFORD_OBS flowsheets, stripped by meds2text's default STARR-OMOP extraction transform for documented timing-bug reasons — a year-old, settled decision). Also found: `parse_lumia` reads a nonexistent `type` attribute (real attr is `table`). **Phil's call:** fix the two bugs, accept the flowsheet exclusion as permanent/expected, re-measure. Adds a new `strip_excluded_lines`/`--exclude-line-patterns` mechanism to `diff_golden.py` (distinct from its line-count-preserving `normalize_text` allowlist) so the byte-diff gate can pass despite the accepted divergence. **Reviewed 2026-07-17** via `/explain-plan` (Phil approved the in-sync HTML). NEXT = VM Phase 0.5 (read-only schema/convention confirmation on `phil-sllm-01`) before the Mac lands the `ehr.py`/`diff_golden.py` fixes. |

**Subsumes:** the *GCP v1_5 multimodal VLM eval stand-up* plan (this roadmap's **Phase 0**) lives on
branch `docs/vlm-eval-gcp-v1_5-standup-plan` (`docs/plans/gcp-vlm-eval-v1_5-multimodal-standup.md`),
Reviewed: No — its v1_5 substrate is **inlined** into the roadmap's Phase 0.
